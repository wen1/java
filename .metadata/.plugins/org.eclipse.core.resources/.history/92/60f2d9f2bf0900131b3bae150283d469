/*
 * Move is for Hanoi tower solution data structure
 * Example:
 * suppose we have 3 towers says 0,1,2 and 3 different size disks 0,1,2
 * all the disks are placed in tower 0, in ascending order from top to base
 *  if we move the disk0 from tower 0, to tower 1
 *  we make a Move(0,0,3) 
 */
public class Move {
	int plate;
	int start;
	int end;
	
	Move(int plateIndex, int startIndex, int endIndex){
		plate = plateIndex;
		start = startIndex;
		end = endIndex;
	}
}
